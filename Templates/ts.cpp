/**
 * $1
 * Created on $CURRENT_YEAR-$CURRENT_MONTH-$CURRENT_DATE
 */

#include <algorithm>
#include <iostream>
#include <string>
#include <vector>
using namespace std;

#define N (1 << 18) // 2e5

int main() {
	cin.tie(0)->sync_with_stdio(0);

	$0
}